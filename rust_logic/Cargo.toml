[package]
name = "cakru_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
jni = "0.21.1"
candle-core = "0.6.0"
candle-transformers = "0.6.0"
tokenizers = { version = "0.19.1", default-features = false }
anyhow = "1.0"
